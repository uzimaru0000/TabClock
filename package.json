{
  "name": "MyNewTab",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "parcel watch -d ./dist --no-source-maps src/index.html",
    "build": "parcel build -d ./dist --no-source-maps src/index.html && cp ./manifest.json ./dist && cp ./icon.png ./dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^1.12.4",
    "typescript": "^3.7.2"
  }
}